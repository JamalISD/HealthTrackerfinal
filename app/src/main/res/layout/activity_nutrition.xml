<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".NutritionActivity">

    <Button
        android:id="@+id/suggestFoodButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Suggest Food"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="16dp"
        android:onClick="onSuggestFoodButtonClick" />

    <TextView
        android:id="@+id/suggestionTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/suggestFoodButton"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="8dp"
        android:text=""
        android:textSize="18sp" />

    <ImageView
        android:id="@+id/mealImageView"
        android:layout_width="332dp"
        android:layout_height="467dp"
        android:layout_below="@+id/suggestionTextView"
        android:layout_alignParentStart="true"
        android:layout_alignParentEnd="true"
        android:layout_alignParentBottom="true"
        android:layout_marginStart="29dp"
        android:layout_marginTop="51dp"
        android:layout_marginEnd="50dp"
        android:layout_marginBottom="117dp"
        android:visibility="visible" />

</RelativeLayout>
